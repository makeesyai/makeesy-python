"""
The reduce() function takes a function, and iterable object (list, tuple, dict, set, frozenset etc),
Takes two values from the iterable object applies the function and store the result in iterable object
as the first value for the second all, this process continue till only one item left in iterable object

NOTE: This is important to note here that, the function strictly should always take two
arguments and return only one value
"""

# List Sum
# Max item of a List
