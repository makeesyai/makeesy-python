"""
The map() function takes a function, and iterable object (list, tuple, dict, set, frozenset etc),
applies the function to each and every item in the given iterable object, and updates the object with
the new values
"""

# Multiply 2, each number of integer list
# Upper Case list of Strings
